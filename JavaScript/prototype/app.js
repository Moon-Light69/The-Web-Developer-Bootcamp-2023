String.prototype.danial = function() {
    console.log(`It's okay but not good enough ---->`, this.toLocaleUpperCase())
}

sarina = "Was it good"

Array.prototype.pop = function() {
    return 'sorry i want that element, i will never pop it off'.toUpperCase()
}

name.__proto__

const navColor = new Color('carrot', [230, 126, 34 ])
const logoColor = new color('emarald', [46, 204, 113 ])

"rgb(244,54,23)"
"rgba(244,54,23,0.9)"
"#76FF32"
"hsl(180, 50%, 30%)"